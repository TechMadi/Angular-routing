/**
 * Importing The angular Router
 * Router Module
 * Service , configuration, directives(routerlink, routerLinkActive, routerOutlet )
 *
 * Importing the router module
 * forRoot()used once application
 * register the router service
 *
 * forChild()
 * does not register the application
 *
 */
